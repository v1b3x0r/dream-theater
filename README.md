# 🌌 DreamSorter: The Entropy Crusher

Tool จัดระเบียบรูปภาพและวิดีโอด้วยพลัง AI (CLIP + Expert LLM)
ออกแบบมาให้ "กันรก" โดยแยก Code ออกจาก Data ชัดเจน

## 📂 Structure
- `inbox/` : โฟลเดอร์ที่พี่เอา "กองขยะรูปภาพ" มาวางกองรวมกันไว้ตรงนี้
- `knowledge_base.json` : สมองของ AI (จำหน้าคน/แมว) *ห้ามลบเล่น!*

## 🚀 Workflow (วิธีใช้)

### Step 1: กวาดขยะขั้นต้น (Cleanup)
คำสั่งนี้จะแยกไฟล์ตามวันที่ (Date Taken / Filename) เข้าไปในโฟลเดอร์ `Sorted` และ `Needs_Review`
```powershell
cmd /c "npm run cleanup -- --target inbox"
```

### Step 2: แยกแยะด้วย AI (The Brain)
สั่งให้ AI เข้าไปจัดการพวกที่ไม่มี Metadata หรือต้องการการแยกแยะพิเศษ
(เข้าไปจัดการใน `Needs_Review` หรือโฟลเดอร์ย่อยๆ)
```powershell
cmd /c "npm start -- --target inbox/Needs_Review/2024/01_Jan"
```

## 🧠 Tips
- **Expert Mode:** ถ้าเจอรูปยากๆ เลือก `🧠 Ask Expert AI` (ต้องเปิด LMStudio พอร์ท 1234 รอไว้)
- **Knowledge:** ไฟล์ `knowledge_base.json` อยู่ที่ root พี่สามารถก๊อปไป Backup ได้
